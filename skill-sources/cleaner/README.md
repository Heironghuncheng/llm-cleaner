# Cleaner Skill Sources

This folder tracks the Cleaner-specific skill source files that should be published with the repository.

Current skills:

- `cleaner-audit`
- `cleaner-clean`
- `cleaner-config`
- `cleaner-first-use`

These are source copies for the repository. Local agent install directories remain under `.agents/skills/` and `.claude/skills/` and are intentionally ignored.
