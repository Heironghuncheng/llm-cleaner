[project]
name = "cleaner"
version = "0.1.0"
description = "Audit and clean leftover software configurations on Windows"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyyaml>=6.0.3",
    "rich>=15.0.0",
    "thefuzz[speedup]>=0.22.1",
]
